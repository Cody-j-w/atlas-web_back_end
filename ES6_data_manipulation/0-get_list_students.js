export default function getListStudents() {
  const s1 = { id: 1, name: 'Guillaume', location: 'San Francisco' };
  const s2 = { id: 2, name: 'James', location: 'Columbia' };
  const s3 = { id: 5, name: 'Serena', location: 'San Francisco' };

  const studentArray = [s1, s2, s3];

  return studentArray;
}
